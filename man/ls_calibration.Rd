% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/libs_calibration-class.R
\name{ls_calibration}
\alias{ls_calibration}
\title{Create a LIBS Calibration Model}
\usage{
ls_calibration(
  element,
  wavelength_nm,
  concentrations,
  intensities,
  model,
  method = "univariate",
  lod = NULL,
  loq = NULL,
  r_squared = NULL,
  unit = "ppm"
)
}
\arguments{
\item{element}{Character. Element symbol (e.g. "Ca", "Fe").}

\item{wavelength_nm}{Numeric. Emission line wavelength used.}

\item{concentrations}{Numeric vector. Known concentrations of standards.}

\item{intensities}{Numeric vector. Measured peak intensities or areas
(or ratios for \code{internal_std}). Same length as \code{concentrations}.}

\item{model}{Fitted model object (e.g. \code{lm}, \code{mvr}).}

\item{method}{Character. One of \code{"univariate"}, \code{"internal_std"}, \code{"pls"},
\code{"cf_libs"}. Default \code{"univariate"}.}

\item{lod}{Numeric. Limit of detection (3-sigma). Default \code{NULL}.}

\item{loq}{Numeric. Limit of quantification (10-sigma). Default \code{NULL}.}

\item{r_squared}{Numeric. Coefficient of determination. Default \code{NULL}.}

\item{unit}{Character. Concentration unit, e.g. \code{"ppm"}, \code{"wt_pct"},
\code{"mg_kg"}. Default \code{"ppm"}.}
}
\value{
A \code{libs_calibration} S3 object.
}
\description{
Constructs a \code{libs_calibration} S3 object holding a fitted calibration
model plus the underlying training data and figures of merit.
}
\examples{
conc <- c(100, 500, 1000, 2500, 5000)
ints <- conc * 0.12 + stats::rnorm(5, 0, 5)
m <- stats::lm(conc ~ ints)
cal <- ls_calibration("Ca", 393.37, conc, ints, model = m,
                      r_squared = summary(m)$r.squared)
print(cal)

}
