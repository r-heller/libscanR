% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/peaks.R
\name{ls_find_peaks}
\alias{ls_find_peaks}
\title{Find Emission Peaks}
\usage{
ls_find_peaks(
  x,
  snr_threshold = 3,
  min_prominence = 0.01,
  min_distance_nm = 0.5
)
}
\arguments{
\item{x}{A \code{\link[=ls_spectrum]{ls_spectrum()}} object.}

\item{snr_threshold}{Numeric. Minimum signal-to-noise ratio. Default 3.}

\item{min_prominence}{Numeric. Minimum peak prominence as a fraction of
maximum intensity (0-1). Default 0.01.}

\item{min_distance_nm}{Numeric. Minimum distance between peaks (nm).
Default 0.5.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} with columns \code{wavelength_nm}, \code{intensity},
\code{snr}, \code{prominence}, \code{fwhm_nm}, \code{area}, sorted by intensity descending.
}
\description{
Detects peaks in a LIBS spectrum using local maxima detection with
prominence and signal-to-noise filtering.
}
\examples{
spec <- ls_simulate_spectrum(elements = c(Ca = 5000, Na = 1000), seed = 1)
peaks <- ls_find_peaks(spec, snr_threshold = 5)
head(peaks)

}
