% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/libs_spectrum-class.R
\name{ls_spectrum}
\alias{ls_spectrum}
\title{Create a LIBS Spectrum Object}
\usage{
ls_spectrum(wavelength, intensity, metadata = list(), baseline = NULL)
}
\arguments{
\item{wavelength}{Numeric vector of wavelengths in nanometers. Must be
sortable and contain no NA.}

\item{intensity}{Numeric vector (single shot) or matrix (multiple shots)
of emission intensities. If matrix: rows = shots, columns = wavelength
channels, \code{ncol(intensity)} must equal \code{length(wavelength)}.}

\item{metadata}{Named list of metadata (e.g. \code{sample_id}, \code{material},
\code{gate_delay_us}, \code{integration_time_us}, \code{laser_energy_mj}, \code{atmosphere},
\code{date}). Default \code{list()}.}

\item{baseline}{Optional numeric vector of baseline values corresponding
to each wavelength channel (populated by \code{\link[=ls_baseline]{ls_baseline()}}). Default \code{NULL}.}
}
\value{
An object of class \code{libs_spectrum}: a named list with elements
\code{wavelength}, \code{intensity} (matrix: shots x channels), \code{metadata},
\code{baseline}, \code{n_shots}, \code{n_channels}, and \code{range_nm}.
}
\description{
Constructs a \code{libs_spectrum} S3 object from a wavelength axis and one or
more intensity vectors (shots).
}
\examples{
wl <- seq(200, 900, length.out = 512)
int <- exp(-((wl - 393)^2) / 2) + stats::rnorm(512, 0, 0.05)
spec <- ls_spectrum(wl, int, metadata = list(sample_id = "demo"))
print(spec)

}
