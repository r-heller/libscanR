% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/libs_dataset-class.R
\name{ls_dataset}
\alias{ls_dataset}
\title{Create a LIBS Dataset}
\usage{
ls_dataset(spectra, sample_info = NULL)
}
\arguments{
\item{spectra}{List of \code{libs_spectrum} objects. All must share the same
wavelength axis (channels and values within 0.1 nm tolerance).}

\item{sample_info}{Optional data.frame or tibble with one row per spectrum.
Must contain column \code{sample_id} matching \code{metadata$sample_id} in each
spectrum. Additional columns (e.g. \code{material}, \code{group}, \code{concentration},
\code{x_pos}, \code{y_pos}) are optional. If NULL, built from spectrum metadata.}
}
\value{
A \code{libs_dataset} S3 object.
}
\description{
Constructs a \code{libs_dataset}: a collection of \code{\link[=ls_spectrum]{ls_spectrum()}} objects
sharing a common wavelength axis, plus a sample-info table.
}
\examples{
specs <- lapply(1:3, function(i) {
  ls_simulate_spectrum(elements = c(Ca = 1000 * i, Na = 500), seed = i)
})
specs <- Map(function(s, id) {
  s$metadata$sample_id <- paste0("s", id)
  s
}, specs, seq_along(specs))
ds <- ls_dataset(specs)
print(ds)

}
