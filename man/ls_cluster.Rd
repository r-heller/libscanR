% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/chemometrics.R
\name{ls_cluster}
\alias{ls_cluster}
\title{Cluster LIBS Spectra}
\usage{
ls_cluster(dataset, method = "kmeans", k = 3, ...)
}
\arguments{
\item{dataset}{A \code{\link[=ls_dataset]{ls_dataset()}} object.}

\item{method}{Character. \code{"kmeans"}, \code{"hclust"}, or \code{"dbscan"}.
Default \code{"kmeans"}.}

\item{k}{Integer. Number of clusters for kmeans/hclust. Default 3.}

\item{...}{Additional arguments passed to the underlying clustering
function.}
}
\value{
An S3 object of class \code{libs_clusters} with elements \code{cluster},
\code{method}, \code{k}, \code{silhouette} (mean silhouette score when computable),
\code{sample_info}.
}
\description{
Unsupervised clustering via k-means, hierarchical, or DBSCAN (DBSCAN
requires the \code{dbscan} package in Suggests).
}
\examples{
ds <- ls_example_data("tissue")
cl <- ls_cluster(ds, method = "kmeans", k = 5)
cl$cluster
}
