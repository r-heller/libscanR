% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calibrate.R
\name{ls_saha_boltzmann}
\alias{ls_saha_boltzmann}
\title{Calibration-Free LIBS via Saha-Boltzmann}
\usage{
ls_saha_boltzmann(
  x,
  elements,
  lines_nm,
  electron_density = 1e+17,
  window_nm = 0.5,
  verbose = TRUE
)
}
\arguments{
\item{x}{A \code{\link[=ls_spectrum]{ls_spectrum()}} object (ideally baseline-corrected).}

\item{elements}{Character vector. Elements to quantify.}

\item{lines_nm}{Named list. Emission lines per element for the Boltzmann
plot, e.g. \code{list(Ca = c(393.37, 396.85, 422.67))}.}

\item{electron_density}{Numeric. Assumed electron density (cm^-3).
Default \code{1e17}.}

\item{window_nm}{Numeric. Integration window per line. Default 0.5.}

\item{verbose}{Logical. Default \code{TRUE}.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} with columns \code{element}, \code{temperature_k},
\code{concentration_rel} (relative mole fraction, sum normalized to 1),
\code{electron_density}, \code{n_lines} (lines used), \code{r_squared} (Boltzmann fit).
}
\description{
Implements the CF-LIBS method for semi-quantitative analysis without
reference standards. Uses the Boltzmann plot method to estimate plasma
temperature and then relative elemental concentrations from line
intensities. Electron density is provided as an input or assumed.
}
\examples{
spec <- ls_simulate_spectrum(
  elements = c(Ca = 5000, Fe = 200, Na = 1000), seed = 2)
spec <- ls_baseline(spec)
ls_saha_boltzmann(spec,
  elements = c("Ca", "Fe"),
  lines_nm = list(Ca = c(422.673, 445.478, 487.813),
                  Fe = c(371.994, 404.581, 438.354)))
}
