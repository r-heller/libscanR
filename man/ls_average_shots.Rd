% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preprocess.R
\name{ls_average_shots}
\alias{ls_average_shots}
\title{Average Replicate Shots}
\usage{
ls_average_shots(x, method = "mean", trim = 0, remove_outliers = FALSE)
}
\arguments{
\item{x}{A \code{\link[=ls_spectrum]{ls_spectrum()}} object.}

\item{method}{Character. \code{"mean"} or \code{"median"}. Default \code{"mean"}.}

\item{trim}{Numeric. Fraction trimmed from each end for trimmed mean.
Default 0.}

\item{remove_outliers}{Logical. Remove shots with deviation > 3 SD from
the per-channel mean. Default \code{FALSE}.}
}
\value{
A \code{libs_spectrum} with a single averaged shot.
}
\description{
Averages shots within a \code{\link[=ls_spectrum]{ls_spectrum()}}.
}
\examples{
spec <- ls_simulate_spectrum(n_shots = 10, seed = 1)
ls_average_shots(spec, remove_outliers = TRUE)

}
