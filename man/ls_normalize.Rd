% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preprocess.R
\name{ls_normalize}
\alias{ls_normalize}
\title{Normalize Spectra}
\usage{
ls_normalize(x, method = "total", ref_wavelength = NULL, ref_window = 1)
}
\arguments{
\item{x}{A \code{\link[=ls_spectrum]{ls_spectrum()}} or \code{\link[=ls_dataset]{ls_dataset()}} object.}

\item{method}{Character. Normalization method. Default \code{"total"}.}

\item{ref_wavelength}{Numeric. Required for \code{method = "internal_std"}.}

\item{ref_window}{Numeric. Window width (nm) around \code{ref_wavelength}.
Default 1.}
}
\value{
Same class as input with normalized intensities.
}
\description{
Normalizes intensity values. Methods:
\itemize{
\item \code{"total"} — divide each shot by total intensity
\item \code{"max"} — divide by per-shot maximum
\item \code{"snv"} — Standard Normal Variate: \code{(x - mean) / sd}
\item \code{"internal_std"} — divide by intensity at a reference line
\item \code{"area"} — divide by integrated (trapezoidal) area
}
}
\examples{
spec <- ls_simulate_spectrum(seed = 1)
spec_n <- ls_normalize(spec, method = "total")
sum(spec_n$intensity[1, ])  # ~1

}
