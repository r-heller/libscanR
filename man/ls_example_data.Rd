% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{ls_example_data}
\alias{ls_example_data}
\title{Generate Example LIBS Dataset}
\usage{
ls_example_data(
  scenario = c("tissue", "calibration", "spatial", "all"),
  seed = 42,
  n_channels = 1024
)
}
\arguments{
\item{scenario}{Character. One of \code{"tissue"}, \code{"calibration"}, \code{"spatial"},
\code{"all"}. Default \code{"tissue"}.}

\item{seed}{Integer. Random seed. Default 42.}

\item{n_channels}{Integer. Number of spectral channels. Default 1024
(reduced from 2048 for faster examples).}
}
\value{
A \code{libs_dataset} object (or named list for \code{"all"}).
}
\description{
Creates a synthetic \code{libs_dataset} suitable for demonstrating package
features. Scenarios:
}
\details{
\itemize{
\item \code{"tissue"} — 50 spectra from 5 tissue types (bone, liver, kidney,
muscle, fat), 10 per type, with tissue-typical elemental signatures.
\item \code{"calibration"} — 27 spectra of Ca standards (9 concentrations x 3
replicates) with constant matrix (Na, K, Mg).
\item \code{"spatial"} — 400 spectra on a 20x20 grid simulating a tissue
cross-section with Ca/Fe gradients and a Zn-enriched hotspot.
\item \code{"all"} — named list of the above.
}
}
\examples{
ds <- ls_example_data("tissue")
print(ds)
}
