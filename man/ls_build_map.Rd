% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/map.R
\name{ls_build_map}
\alias{ls_build_map}
\title{Build a Spatial Elemental Map}
\usage{
ls_build_map(dataset, element, line_nm, calibration = NULL, window_nm = 1)
}
\arguments{
\item{dataset}{A \code{\link[=ls_dataset]{ls_dataset()}} object with spatial coordinates.}

\item{element}{Character. Element label (for metadata).}

\item{line_nm}{Numeric. Emission line wavelength (nm).}

\item{calibration}{Optional \code{\link[=ls_calibration]{ls_calibration()}}. If provided, intensities
are converted to concentration. Default \code{NULL}.}

\item{window_nm}{Numeric. Peak integration window. Default 1.}
}
\value{
A list with S3 class \code{libs_map} containing \code{x}, \code{y}, \code{values},
\code{element}, \code{line_nm}, \code{unit}, and a rasterized \code{grid} matrix when the
points form a regular grid.
}
\description{
Constructs a spatial map from LIBS line-scan or raster-scan data. The
dataset's \code{sample_info} must include \code{x_pos} and \code{y_pos} columns.
}
\examples{
ds <- ls_example_data("spatial")
m <- ls_build_map(ds, "Ca", 393.37)
dim(m$grid)
}
