% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io-read.R
\name{ls_read_dir}
\alias{ls_read_dir}
\title{Read All Spectra from a Directory}
\usage{
ls_read_dir(
  dir,
  pattern = "\\\\.(csv|tsv|txt)$",
  recursive = FALSE,
  sample_info = NULL,
  ...
)
}
\arguments{
\item{dir}{Character. Directory path.}

\item{pattern}{Character. File pattern (regex). Default \code{"\\\\.(csv|tsv|txt)$"}.}

\item{recursive}{Logical. Recurse into subdirectories. Default \code{FALSE}.}

\item{sample_info}{data.frame/tibble. Optional sample metadata table.
Must have a \code{sample_id} column matching (or being a subset of) the
filenames without extension. Default \code{NULL}.}

\item{...}{Additional arguments passed to \code{\link[=ls_read_spectrum]{ls_read_spectrum()}}.}
}
\value{
A \code{libs_dataset} object.
}
\description{
Batch-imports spectrum files from a directory into a \code{libs_dataset}.
}
\examples{
dir <- tempfile()
dir.create(dir)
for (i in 1:3) {
  wl <- seq(200, 900, length.out = 100)
  int <- exp(-((wl - 393)^2) / 2) + stats::rnorm(100, 0, 0.01)
  utils::write.csv(data.frame(w = wl, i = int),
                   file.path(dir, paste0("s", i, ".csv")),
                   row.names = FALSE)
}
ds <- ls_read_dir(dir, verbose = FALSE)
unlink(dir, recursive = TRUE)

}
