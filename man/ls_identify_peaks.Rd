% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/peaks.R
\name{ls_identify_peaks}
\alias{ls_identify_peaks}
\title{Identify Peaks Against the NIST Line Database}
\usage{
ls_identify_peaks(
  peaks,
  elements = NULL,
  tolerance_nm = 0.2,
  ionization = c(1L, 2L)
)
}
\arguments{
\item{peaks}{A tibble from \code{\link[=ls_find_peaks]{ls_find_peaks()}}.}

\item{elements}{Character vector. Restrict search. Default \code{NULL} = all.}

\item{tolerance_nm}{Numeric. Maximum wavelength deviation. Default 0.2.}

\item{ionization}{Integer vector of ionization states. Default \code{c(1L, 2L)}.}
}
\value{
A tibble with all peak columns plus: \code{element}, \code{ionization},
\code{nist_wavelength_nm}, \code{nist_aki}, \code{deviation_nm}, \code{confidence} (0-1).
}
\description{
Matches detected peaks to known atomic emission lines. Each peak is
assigned its best-scoring candidate from the curated NIST database.
}
\examples{
spec <- ls_simulate_spectrum(elements = c(Ca = 5000, Na = 1000), seed = 1)
pk <- ls_find_peaks(spec, snr_threshold = 5)
id <- ls_identify_peaks(pk, elements = c("Ca", "Na"))
head(id)
}
