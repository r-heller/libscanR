% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calibrate.R
\name{ls_calibrate}
\alias{ls_calibrate}
\title{Build Calibration Curve}
\usage{
ls_calibrate(
  dataset,
  element,
  line_nm,
  concentrations,
  method = "univariate",
  internal_std_nm = NULL,
  window_nm = 1,
  pls_window_nm = NULL,
  n_components = 5,
  unit = "ppm",
  verbose = TRUE
)
}
\arguments{
\item{dataset}{A \code{\link[=ls_dataset]{ls_dataset()}} containing standard spectra. Spectra
order must correspond to \code{concentrations}.}

\item{element}{Character. Target element symbol.}

\item{line_nm}{Numeric. Emission line wavelength (nm).}

\item{concentrations}{Numeric vector of known concentrations, one per
spectrum in \code{dataset}.}

\item{method}{Character. \code{"univariate"} (default), \code{"internal_std"},
\code{"pls"}, or \code{"cf_libs"}.}

\item{internal_std_nm}{Numeric. Internal standard line wavelength,
required for \code{method = "internal_std"}.}

\item{window_nm}{Numeric. Peak integration window. Default 1.}

\item{pls_window_nm}{Numeric vector of length 2. Spectral window for PLS.
Default \code{c(line_nm - 5, line_nm + 5)}.}

\item{n_components}{Integer. Number of PLS components. Default 5.}

\item{unit}{Character. Concentration unit. Default \code{"ppm"}.}

\item{verbose}{Logical. Default \code{TRUE}.}
}
\value{
A \code{\link[=ls_calibration]{ls_calibration()}} object.
}
\description{
Constructs a calibration model from standards with known concentrations.
}
\examples{
ds <- ls_example_data("calibration")
conc <- ds$sample_info$concentration
cal <- ls_calibrate(ds, "Ca", 393.37, conc, verbose = FALSE)
print(cal)
}
