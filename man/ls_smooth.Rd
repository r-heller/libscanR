% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preprocess.R
\name{ls_smooth}
\alias{ls_smooth}
\title{Smooth Spectra}
\usage{
ls_smooth(x, method = "savgol", window = 11, poly_order = 3)
}
\arguments{
\item{x}{A \code{\link[=ls_spectrum]{ls_spectrum()}} or \code{\link[=ls_dataset]{ls_dataset()}} object.}

\item{method}{Character. Default \code{"savgol"}.}

\item{window}{Integer. Window size in channels (odd preferred). Default 11.}

\item{poly_order}{Integer. Polynomial order for Savitzky-Golay. Default 3.}
}
\value{
Same class as input with smoothed intensities.
}
\description{
Applies spectral smoothing. Methods: \code{"savgol"} (Savitzky-Golay, needs
the \code{signal} package; otherwise falls back to moving average with a
warning), \code{"moving_avg"}, \code{"gaussian"}, \code{"median"}.
}
\examples{
spec <- ls_simulate_spectrum(seed = 1)
sm <- ls_smooth(spec, method = "moving_avg", window = 9)

}
