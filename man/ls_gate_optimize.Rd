% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preprocess.R
\name{ls_gate_optimize}
\alias{ls_gate_optimize}
\title{Gate Delay Optimization}
\usage{
ls_gate_optimize(spectra, element, line_nm, window_nm = 1)
}
\arguments{
\item{spectra}{List of \code{\link[=ls_spectrum]{ls_spectrum()}} objects. Each spectrum's metadata
should contain \code{gate_delay_us}.}

\item{element}{Character. Target element symbol.}

\item{line_nm}{Numeric. Target emission line wavelength (nm).}

\item{window_nm}{Numeric. Window around the line. Default 1.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} with columns \code{gate_delay_us}, \code{snr}, \code{sbr},
\code{peak_intensity}, with attribute \code{"recommended"} giving the row index
of the highest SNR.
}
\description{
Analyzes SNR and signal-to-background ratios across spectra acquired at
different gate delays to recommend optimal timing for a target line.
}
\examples{
specs <- lapply(c(0.5, 1, 2, 5, 10), function(g) {
  s <- ls_simulate_spectrum(seed = round(g * 10))
  s$metadata$gate_delay_us <- g
  s
})
ls_gate_optimize(specs, "Ca", 393.37)
}
