% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/chemometrics.R
\name{ls_train_classifier}
\alias{ls_train_classifier}
\title{Train a Classifier}
\usage{
ls_train_classifier(dataset, grouping, method = "svm", ...)
}
\arguments{
\item{dataset}{A \code{\link[=ls_dataset]{ls_dataset()}} with labelled samples.}

\item{grouping}{Character. Column name in \code{sample_info} with class labels.}

\item{method}{Character. \code{"svm"} or \code{"rf"}. Default \code{"svm"}.}

\item{...}{Additional arguments passed to the underlying trainer.}
}
\value{
An S3 object of class \code{libs_classifier}.
}
\description{
Trains SVM or Random Forest on spectral intensity features.
}
\examples{
ds <- ls_example_data("tissue")
\donttest{
if (requireNamespace("e1071", quietly = TRUE)) {
  clf <- ls_train_classifier(ds, "tissue", method = "svm")
  clf$accuracy
}
}
}
