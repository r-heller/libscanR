% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{ls_simulate_spectrum}
\alias{ls_simulate_spectrum}
\title{Simulate a LIBS Spectrum}
\usage{
ls_simulate_spectrum(
  elements = c(Ca = 5000, Fe = 200, Na = 1000),
  wavelength_range = c(200, 900),
  n_channels = 2048,
  n_shots = 10,
  noise_level = 0.02,
  continuum_level = 100,
  shot_rsd = 0.1,
  fwhm_nm = 0.1,
  seed = NULL
)
}
\arguments{
\item{elements}{Named numeric vector. Element concentrations (arbitrary
units, interpreted as relative intensity scaling). Default
\code{c(Ca = 5000, Fe = 200, Na = 1000)}.}

\item{wavelength_range}{Numeric vector of length 2. Range in nm.
Default \code{c(200, 900)}.}

\item{n_channels}{Integer. Number of spectral channels. Default 2048.}

\item{n_shots}{Integer. Number of replicate shots. Default 10.}

\item{noise_level}{Numeric. Relative noise level (fraction of max
intensity). Default 0.02.}

\item{continuum_level}{Numeric. Continuum background level. Default 100.}

\item{shot_rsd}{Numeric. Relative standard deviation of shot-to-shot
intensity variation (0-1). Default 0.1.}

\item{fwhm_nm}{Numeric. Typical line FWHM in nm. Default 0.1.}

\item{seed}{Optional integer random seed for reproducibility.}
}
\value{
A \code{\link[=ls_spectrum]{ls_spectrum()}} object.
}
\description{
Generates a realistic synthetic LIBS spectrum with configurable elemental
composition, noise, and continuum background. Emission lines are drawn
from the internal NIST database (\code{\link[=ls_element_db]{ls_element_db()}}) and modelled as
Lorentzian peaks.
}
\examples{
spec <- ls_simulate_spectrum(elements = c(Ca = 1000, Na = 500), seed = 1)
print(spec)
}
