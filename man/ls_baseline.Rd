% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preprocess.R
\name{ls_baseline}
\alias{ls_baseline}
\title{Baseline Correction}
\usage{
ls_baseline(
  x,
  method = "snip",
  iterations = 100,
  order = 3,
  lambda = 1e+05,
  p = 0.01,
  radius = 50,
  ...
)
}
\arguments{
\item{x}{A \code{\link[=ls_spectrum]{ls_spectrum()}} or \code{\link[=ls_dataset]{ls_dataset()}} object.}

\item{method}{Character. One of \code{"snip"}, \code{"als"}, \code{"rolling_ball"},
\code{"linear"}, \code{"polynomial"}. Default \code{"snip"}.}

\item{iterations}{Integer. Iterations for iterative methods (SNIP, ALS).
Default 100.}

\item{order}{Integer. Polynomial order for \code{"polynomial"}. Default 3.}

\item{lambda}{Numeric. Smoothness parameter for ALS. Default \code{1e5}.}

\item{p}{Numeric. Asymmetry parameter for ALS (0 < p < 1). Default 0.01.}

\item{radius}{Integer. Rolling-ball radius in channels. Default 50.}

\item{...}{Reserved for future use.}
}
\value{
Same class as input with baseline-corrected intensities; the
estimated baseline is stored in the \code{baseline} element for inspection.
}
\description{
Removes continuum/baseline drift from LIBS spectra. Supported methods:
\code{"snip"} (Statistics-sensitive Non-linear Iterative Peak-clipping,
Morhac/Ryan), \code{"als"} (Asymmetric Least Squares smoothing, Eilers &
Boelens), \code{"rolling_ball"} (morphological), \code{"linear"} (two-endpoint
interpolation), or \code{"polynomial"} (low-order polynomial fit of
baseline-only regions).
}
\examples{
spec <- ls_simulate_spectrum(seed = 1)
corr <- ls_baseline(spec, method = "snip")
is.null(corr$baseline)

}
