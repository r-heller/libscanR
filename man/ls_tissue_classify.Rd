% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tissue.R
\name{ls_tissue_classify}
\alias{ls_tissue_classify}
\title{Tissue Classification via LIBS}
\usage{
ls_tissue_classify(
  x,
  method = "ratio",
  reference = NULL,
  group_col = "tissue",
  verbose = TRUE
)
}
\arguments{
\item{x}{A \code{\link[=ls_spectrum]{ls_spectrum()}} or \code{\link[=ls_dataset]{ls_dataset()}} object (unknowns).}

\item{method}{Character. \code{"ratio"}, \code{"plsda"}, or \code{"svm"}. Default \code{"ratio"}.}

\item{reference}{A \code{\link[=ls_dataset]{ls_dataset()}} with labelled tissue reference spectra
(needed for \code{"plsda"} and \code{"svm"}).}

\item{group_col}{Character. Column in reference \code{sample_info} with tissue
labels. Default \code{"tissue"}.}

\item{verbose}{Logical. Default \code{TRUE}.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} with columns \code{sample_id}, \code{predicted_tissue},
\code{confidence}.
}
\description{
Classifies tissue types based on LIBS spectral signatures. Implements
three methods:
\itemize{
\item \code{"ratio"} — rule-based using canonical elemental ratios (Ca/Na, Fe/Na,
Zn/Na, K/Na, C/Ca) derived from tissue biochemistry. No training data
required.
\item \code{"plsda"} — fits a PLS-DA model on the supplied labelled \code{reference}
dataset and predicts class for \code{x}.
\item \code{"svm"} — fits an SVM on \code{reference} and predicts class.
}
}
\examples{
ds <- ls_example_data("tissue")
ls_tissue_classify(ds[1:3])
}
